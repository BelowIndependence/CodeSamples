game:GetService("Players").PlayerAdded:Connect(function(plr)
	plr.Chatted:Connect(function(msg)
		print(msg)
	end)
end)

task.spawn(function()
    while task.wait() do
        print("LOL")
    end
end)
